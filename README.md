## Start

  

```javascript

// in the base app (main directory: companyx-onlinelibrary) run command prompt
npm  install

// in the client (sub directory: client) run command prompt
npm  install
  
//open 2 terminals or command prompts na ewan
//to run the front end 
npm run client
//to run the back end
npm run server

// Server runs on http://localhost:5000 and client on http://localhost:3000